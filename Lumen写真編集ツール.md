# Lumen（写真編集・キャプションツール）

不動産登録用に、物件写真の編集とキャプション作成を行うブラウザアプリ。  
杏栄（ハウスドゥ新大阪北店まわり）の運用を想定。

**最終更新:** 2026-09-02

## リンク

- GitHub（public）: https://github.com/erimizoguchi-star/lumen-photo
- ローカルフォルダ: `/Users/erimizoguchi/Desktop/photo`
- 起動URL: http://127.0.0.1:5173/（**サーバー起動後のみ**有効）
- 起動ファイル: `開く.command`（Finder でダブルクリック）

## Obsidianから開く

Obsidian のリンクだけではサーバーが起動しないため、下の **Lumen起動.app** を使います。

1. **[[Lumen起動.app]]** をクリック（Vault 直下に配置）
2. 初回は macOS が確認を出す → **開く**
3. サーバー起動後、**Google Chrome** で Lumen が開きます

> URL（`http://127.0.0.1:5173/`）を Obsidian から開いても、サーバーが止まっているとつながりません。必ず **Lumen起動.app** から起動してください。

## 起動方法

```bash
# 方法A（推奨）: Finder で 開く.command をダブルクリック
# → 黒いターミナルウィンドウは閉じないこと（閉じるとサーバー停止）

# 方法B: ターミナル
cd /Users/erimizoguchi/Desktop/photo
ruby -run -e httpd . -p 5173
open -a "Google Chrome" http://127.0.0.1:5173/
```

### 再読み込み・つながらないとき

| 症状 | 対処 |
|------|------|
| ページが真っ白 / つながらない | `開く.command` または **[[Lumen起動.app]]** を再実行 |
| Obsidian から ERR_CONNECTION_REFUSED | **[[Lumen起動.app]]** を使う（URL直リンクはサーバー未起動だと失敗） |
| 古い画面のまま | 画面上部の **「再読み込み」** または Cmd+Shift+R |
| 写真が読み込めない | JPEG/PNG を試す。HEIC は **Safari** 推奨 |
| 読み込み中のまま止まる | ページ再読み込み → 写真枚数を減らして再試行 |

※ `index.html` を直接開くと CSS/JS が読み込まれない。必ずローカルサーバー経由。

## できること（機能一覧）

### 操作パネル（4タブ）

左パネルは **写真 / 編集 / キャプション / 出力** の4タブに分かれています。タブは上部に固定され、中身だけスクロールします。

| タブ | 内容 |
|------|------|
| 写真 | 追加・ギャラリー・名前・物件種別・住所 |
| 編集 | サイズ・トリミング・角度・隠す・明るさ・空 |
| キャプション | AI生成・定型文・コピー |
| 出力 | 透かし・ロゴ位置・保存フォルダ・同名ファイル上書き |

※ 写真未読み込み時は「編集」「キャプション」タブは無効です。

### 画面上部の操作

| ボタン | 内容 |
|--------|------|
| **家アイコン** | 初期画面に戻す（写真・住所・キャプションをすべてクリア） |
| 再読み込み | 最新版を読み込み（キャッシュ回避） |
| リセット | 選択中写真の編集を元に戻す |
| 保存 / ダウンロード | フォルダ保存またはブラウザDL |

左上の **v20** 表示でバージョンを確認できます。

### 写真タブの操作

| 操作 | 内容 |
|------|------|
| ゴミ箱アイコン（物件情報横） | 住所・名前・キャプションのみクリア（写真は残す） |
| すべて削除 | 写真をすべて削除 |

### 写真の読み込み

| 機能 | 内容 |
|------|------|
| 複数枚アップロード | ドラッグ＆ドロップ / ファイル選択（追加式） |
| 読み込み表示 | スピナー・進捗バー・枚数表示 |
| 対応形式 | JPEG / PNG / WEBP など（HEIC は Chrome 非対応） |
| 大きい写真 | 長辺 8192px 超は自動縮小して表示 |

### 写真編集

| 機能 | 内容 |
|------|------|
| サイズ変更 | 1枚適用 / 全枚一括（枠内・幅固定・高さ固定・長辺・強制） |
| IRI登録サイズ | 見出写真（50–90px・5KB）/ 物件写真（400–800px・250KB）プリセット |
| トリミング | 枠ドラッグ、比率指定 |
| 角度 | 微調整・90°回転、適用後に余白切り取り |
| 隠す | ブラシでモザイク／復元ブラシで一部だけ元に戻す |
| 自動モザイク | 顔・人物・ナンバー（端末内AI＋Gemini併用で精度向上） |
| モザイク元戻し | 直前の状態に一括復元 |
| 明るさ | 明るさ・コントラスト |
| 空の編集 | フォトショップ風の空置き換え（プリセット・調整・前景の光） |
| ズーム / 移動 | ピンチ・ホイール・移動モード |
| 透かし | KYOUEI ロゴ（ON/OFF・4隅から位置選択・写真ごと） |
| 保存 | 設定した名前で `.jpg` 保存（重複時はエラー表示） |
| 上書き保存 | 出力タブで「同名ファイルを上書き保存」ON で既存ファイルを置換 |
| 高速保存 | 透かし・明るさのみのときは大きな写真でもフリーズしにくい |
| 容量最適化 | IRI上限に近いサイズまで自動圧縮 |
| 一括 | サイズ適用して保存 / DL |

### 空の編集（概要）

- **空の選択:** 晴れ / 青空 / 夕焼け / 曇り など8種のプリセット
- **空の調整:** 不透明度・明るさ・色温度・スケール・位置
- **境界・前景:** 検出範囲・境界のなじませ・前景の光・雲の活かし
- 画像上部からつながる空だけを対象（建物の同系色は除外）

### キャプション（登録サイト用）

| 項目 | 内容 |
|------|------|
| プレフィックス | 先頭に必ず `《杏栄》` |
| 文字数 | **《杏栄》込みで合計20文字以内**（本文は最大16文字） |
| 物件種別 | マンション売買 / 戸建て売買 / 土地売買 |
| 住所 | 任意。周辺環境コメントの参考（徒歩分数は断定しない） |
| カテゴリ | 生成前に必須選択（種別ごとに一覧が変わる） |
| AI生成 | Google Gemini（要APIキー・写真観察ベースで精度向上） |
| 定型文 | 種別・カテゴリ別の短い定型 |
| 出力 | 1件コピー / 全件コピー / txt一覧 |

## キャプション運用ルール

1. **物件種別**を選ぶ（マンション / 戸建て / 土地）
2. 必要なら **物件住所** を入れる
3. 写真ごとに **カテゴリを先に選ぶ**（例: キッチン、外観、現地）
4. 「この写真から生成」または定型文
5. 登録サイトの写真コメント欄へ貼り付け

### 種別ごとのカテゴリ傾向

| 種別 | 主なカテゴリ |
|------|----------------|
| マンション | 外観・室内・バルコニー・共用部・周辺環境 など |
| 戸建て | 外観・室内・庭・駐車場・周辺環境 など（共用部なし） |
| 土地 | 現地・前面道路・整形地・建築向き・周辺環境 など（室内なし） |

### 出力例

```text
《杏栄》対面キッチン
《杏栄》明るいリビング
《杏栄》整った現地
```

## AI生成（Gemini）

- APIキー取得: https://aistudio.google.com/apikey
- キーはブラウザの `localStorage` にのみ保存
- 「確認」ボタンでキー有効性をチェック（一覧APIのみ・生成はしない）
- 写真は生成時に Google へ送信される
- モデル優先: `gemini-2.5-flash` → `gemini-2.0-flash` → lite 系（上限時）

### キャプション精度を上げるコツ

- **カテゴリを必ず先に選ぶ**（最重要）
- 物件種別を正しく設定する
- 暗い写真は先に明るさ調整してから生成
- 編集後の画像（明るさ・空など）がそのまま AI に送られる

### よくあるエラー

| メッセージ | 意味・対処 |
|------------|------------|
| APIキーが無効 | キー再発行・打ち間違い確認 |
| 利用上限に達しています | 無料枠超過。1〜2分待つ / 使用量確認 / 課金 |
| リファラー制限 | キー制限を「なし」にするか `http://127.0.0.1:5173/*` を許可 |
| 先にカテゴリを選んで… | カテゴリ未選択 |
| HEICはChromeでは読み込めません | Safari で開くか JPEG に変換 |

## 自動モザイク

| 対象 | 方式 |
|------|------|
| 顔・人物 | COCO-SSD（mobilenet_v2）＋ BlazeFace |
| ナンバー | Gemini API キーあり → AI 検知 / 色ベース検知の併用 |

- **モザイクを元に戻す:** 手動・自動モザイク直前の状態に一括復元
- **復元ブラシ:** かけ過ぎた部分だけ元に戻す

### モザイク関連エラー

| メッセージ | 対処 |
|------------|------|
| スクリプト読込失敗（blazeface） | ページ再読み込み。ネット接続確認 |
| 検知モデルの読込に失敗 | しばらく待って再試行 |

## 出力設定

- **会社ロゴ透かし:** KYOUEI ロゴ（デフォルト ON）
- **ロゴ位置:** 左上 / 右上 / 左下 / 右下（写真ごとに変更可）
- プレビュー・保存・ダウンロード時に反映
- ロゴファイル: `assets/kyouei-watermark.png`
- **同名ファイルを上書き保存:** デフォルト ON。OFF にすると同名ファイルがあると保存を中断

### 高速保存（透かし・明るさのみ）

サイズ変更やモザイクをしていない場合、保存時に元写真を直接縮小して透かし・明るさを反映します。

| 高速保存 OK | 通常の保存処理 |
|------------|---------------|
| 透かしのみ | モザイク・トリミング・サイズ変更あり |
| 明るさ変更 | コントラスト変更 |
| | 空の編集 |

保存中は「透かしを付けて書き出し中…」と表示されます。

### ファイル名

- 保存・ダウンロード時は **左パネル「写真」タブで設定した名前** をそのまま使用（例: `外観.jpg`）
- 末尾に番号やタイムスタンプは付けない
- 複数枚で名前が重複する場合は保存を中断し、名前変更を促す

### IRI 登録サイズ（公式ガイドライン準拠）

| プリセット | サイズ | 上限 |
|-----------|--------|------|
| 見出写真 | 50–90px | 5KB |
| 物件写真 | 400–800px | 250KB |

- 編集タブ「サイズ」内でプリセット選択
- 保存時に上限に近いサイズまで自動圧縮
- [IRI 画像ガイドライン（PDF）](https://www.iri.ne.jp/kensaku/member/info_supo/images/Guideline.pdf)

## ファイル構成

| ファイル | 役割 |
|----------|------|
| `index.html` | UI |
| `styles.css` | スタイル |
| `app.js` | 編集・一括・キャプション・Gemini・空・透かし |
| `assets/kyouei-watermark.png` | 透かしロゴ |
| `開く.command` | ローカルサーバー起動＋Chrome で開く |
| `Lumen起動.app` | Obsidian からワンクリック起動（サーバー＋Chrome） |
| `scripts/lumen-open.sh` | 起動スクリプト（アプリから呼び出し） |
| `scripts/Lumen起動.applescript` | 上記アプリのソース |

依存: Node 不要。Ruby の簡易 HTTP サーバーで動作。推奨ブラウザは **Google Chrome**。

## 関連

- [[不動産一括登録ツール]] … サイトへの半自動登録（写真枚数上限あり）
- 参考サイト: HOUSEDO新大阪北店（物件写真コメントのトーン参考）

## 変更履歴（メモ）

- 2026-09-02: 初期画面に戻すホームアイコン、物件情報クリアアイコン（v20）
- 2026-09-02: 透かし・明るさのみの高速保存、同名ファイル上書き保存
- 2026-09-02: Obsidian 起動用 `Lumen起動.app`、左パネル4タブ化
- 2026-09-02: IRI登録サイズプリセット、容量上限に近い自動圧縮
- 2026-09-02: 保存ファイル名を設定名のみに（重複チェック）
- 2026-09-02: 透かし4隅配置、モザイク元戻し・復元ブラシ、自動モザイク精度向上
- 2026-09-02: 読み込み中表示、再読み込みボタン、HEIC 対応メッセージ
- 2026-09-02: 空の編集（フォトショップ風）、KYOUEI 透かし、キャプション精度改善
- 2026-09-01: 空の自動検知・建物除外マスク、BlazeFace CDN 修正

## 今後の検討メモ

- キャプションと一括登録ツールの連携
- HEIC を Chrome でも読めるようにする（変換ライブラリ）
- 定型文・種別カテゴリの店舗向けカスタム
